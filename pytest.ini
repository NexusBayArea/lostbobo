[pytest]
addopts = -q --tb=short --strict-markers
testpaths = tests
asyncio_mode = auto

markers =
    dag: DAG validation tests
    runtime: runtime execution tests
    trace: trace determinism tests
