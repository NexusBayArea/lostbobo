name: Auto-Deploy to RunPod (Post-Image-Push)

on:
  workflow_run:
    workflows: ["Deploy to Docker Hub"]
    types: [completed]
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  auto-deploy-runpod:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Restart RunPod to pull new image
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
          RUNPOD_ID: ${{ secrets.RUNPOD_ID }}
        run: |
          echo "Triggering Pod Restart for ID: $RUNPOD_ID"
          response=$(curl -s -X POST "https://api.runpod.io/graphql" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $RUNPOD_API_KEY" \
            -d "{\"query\": \"mutation { podRestart(podId: \\"$RUNPOD_ID\\") { id status } }\"}")
          
          if echo "$response" | grep -q "\"errors\""; then
            echo "Deployment Failed: $response"
            exit 1
          fi
          echo "Pod restarted successfully. Pulling simhpc-unified:latest..."

      - name: Wait for pod to be ready
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
          RUNPOD_ID: ${{ secrets.RUNPOD_ID }}
        run: |
          attempt=0
          while [ $attempt -lt 20 ]; do
            attempt=$((attempt + 1))
            status_response=$(curl -s -X POST "https://api.runpod.io/graphql" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $RUNPOD_API_KEY" \
              -d "{\"query\": \"query { pod(input: { podId: \\"$RUNPOD_ID\\"}) { id status } }\"}")
            current_status=$(echo "$status_response" | grep -o "\"status\":\"[^\"]*\"" | head -1 | cut -d"\"" -f4)
            if [ "$current_status" = "RUNNING" ]; then
              echo "Pod is RUNNING."
              exit 0
            fi
            sleep 10
          done
          exit 1
