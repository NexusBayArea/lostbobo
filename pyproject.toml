[project]
name = "simhpc"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["tools", "tools.*"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
